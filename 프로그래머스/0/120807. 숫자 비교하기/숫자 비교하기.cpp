#include <string>
#include <vector>

using namespace std;

int solution(int num1, int num2) {
    int answer = 0;
    
    // Upload Test
    
    answer = num1 == num2 ? 1 : -1;
    
    return answer;
}